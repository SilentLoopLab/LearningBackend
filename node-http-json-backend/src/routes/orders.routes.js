const parseUrl = require("../utils/parseURL.js");

const oredersRoutes = (req, res, endpoint, id) => {
    const method = req.method;

    if (method === "GET") {
    } else if (method === "POST") {
    } else if (method === "PUT") {
    } else if (method === "PATCH") {
    } else if (method === "DELETE") {
    }
};

module.exports = oredersRoutes;
